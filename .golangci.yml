issues:
  exclude-dirs:
    - lib
  exclude-files:
    - xxhash.go
  exclude-rules:
    - path: token.go
      linters:
        - errcheck
    - path: escape.go
      linters:
        - errcheck

    - linters:
        - staticcheck
      text: "SA9003"
